# ML-KEM, ML-DSA, and SPHINCS+

> The PQC algorithms in the stack and what each is for (KEM vs signatures).
>
> TODO before writing: verify which of these actually run on the Egret build. The Egret datasheet names only SPHINCS+ for PQ-secure boot; confirm whether ML-KEM and ML-DSA run on the acc or the Pavona crypto library on Egret before promising a runnable artifact. Three algorithm families in one ~3-hour session is dense; scope to what is runnable and keep the rest a lighter survey.
