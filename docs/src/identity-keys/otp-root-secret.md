# The OTP root secret

> The OTP root secret and how it seeds everything downstream. Read otp_ctrl and the identities/root-keys spec.
>
> TODO before writing: keymgr derivation consumes the lifecycle state alongside this OTP secret. The "Secure storage and lifecycle" Part now precedes this one, so build on the lifecycle states the reader just met rather than re-introducing them.
